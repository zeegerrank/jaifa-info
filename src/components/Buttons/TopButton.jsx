import React from "react";
import { Button } from "react-bootstrap";

const TopButton = () => {
    const scrollTop = ()  => {
        
    }
  return <Button onClick={} className="">TopButton</Button>;
};

export default TopButton;
